# Per-machine configuration for the tv-shell v2 core (`tv-shell-core`).
#
# THIS IS A SEPARATE FILE FROM v1's config.toml, AND IT MUST STAY THAT WAY.
# The v1 daemon's root config struct is `deny_unknown_fields`, so adding a
# [display] or [session] table to config.toml would make the **v1 daemon abort
# at startup** — and the symptom presents as "v1 is broken", not "someone added
# a v2 table". v1 has to keep booting on the couch while v2 is built beside it
# (docs/V2_DESIGN.md §11, "beside, not instead, at every shared layer"), so the
# core reads its own file. Nothing here is ever read by v1, and nothing v1 reads
# is ever written here. Pointing the core at config.toml fails loudly on the
# first v1 table rather than quietly running all-defaults.
#
# Copy to ~/.config/tv-shell/core.toml and edit. EVERYTHING here is OPTIONAL —
# with no core.toml at all the core boots on the defaults below, which are the
# measured-good values for the reference chain (HTPC → AVR → 4K120 OLED). Every
# key is shown commented out with its default.
#
# This file is parsed with `deny_unknown_fields` at every level, so a typo
# (`refres = 120`) is a startup failure naming the bad key rather than a setting
# that silently did nothing. `validate()` then rejects values that would fail
# confusingly later, with a `config: ...` message naming the value.
#
# Path overrides (runtime/session only, not per-machine config):
#   TV_SHELL_CORE_CONFIG  — the path to this file
#   TV_SHELL_CORE_SOCK    — the IPC socket path
#                           (default /run/user/<uid>/tv-shell-core.sock —
#                            deliberately NOT tv-shell-input.sock, so a stray v1
#                            client can never be answered by the v2 grammar)

# --- Output mode and HDR ------------------------------------------------------
[display]
# THESE ARE LIVE. The session script runs `tv-shell-core write-session-env`
# before starting the target; that renders these keys into
# $XDG_RUNTIME_DIR/tv-shell-gamescope-mode, and
# core/units/tv-shell-gamescope.service reads that file and substitutes
# ${TV_SHELL_GS_WIDTH} / _HEIGHT / _REFRESH into gamescope's command line.
# Change a value here and the compositor starts at it.
#
# It was not always so: the unit hard-coded `-W 3840 -H 2160 -r 120` while these
# keys documented themselves as "read by the unit's ExecStart". They were not —
# setting `refresh = 60` was accepted by validate() and changed nothing at all.
# Two literals with no link between them. The env file is the link.
#
# PINNED, not negotiated: the EDID preferred mode on this chain is 60 Hz
# (V2_DESIGN §6), so letting the display choose gives 4K60, not 4K120.
#width = 3840
#height = 2160
#refresh = 120

# Whether HDR is on.
#
# LIVE. `hdr = false` renders an EMPTY TV_SHELL_GS_HDR_ARGS and gamescope starts
# without --hdr-enabled. The unit used to pass --hdr-enabled unconditionally
# while this key documented itself as read by nothing, so `hdr = false` got you
# HDR anyway; the measured kit gates the flag on TV_SHELL_GS_HDR for the same
# reason this now does.
#
# It is a START-TIME setting, not a runtime toggle: changing it needs a session
# restart, because the core writes no HDR atom yet. When the runtime surface
# lands (§6) it will be a ROOT-ATOM WRITE read back via
# GAMESCOPE_HDR_OUTPUT_FEEDBACK — and NEVER a bare `gamescopectl <convar>`. On
# 3.16.x and master a value-less convar call RESETS the convar to its default and
# turns HDR off with no log line at all; that is how the phase-2 "feedback 0" was
# self-inflicted.
#hdr = true

# --hdr-sdr-content-nits: the white point SDR content (the shell itself) is
# mapped to inside an HDR output. Passed with --hdr-enabled, as one pair.
#
# NOT A SETTLED NUMBER. §6 makes the black floor an eyes-only criterion and names
# gamescope#1887 (SDR oversaturated on an HDR output) as the risk for the shell's
# own colours. 200 is the only value ever run on this chain
# (dev/gamescope/session.sh:51). It was 400 here — a number nobody had looked at
# through the television — and is now the measured one. Expect to move it.
#sdr_nits = 200

# Whether VRR (--adaptive-sync) is on.
#
# §13 Q11 IS OPEN AND THIS KEY EXISTS SO NOTHING CLOSES IT BY ACCIDENT. §6 and
# the ops record both note OLED near-black flicker and AVR OSD problems with VRR
# on this exact chain and lean toward off; the week-long live measurement
# nonetheless ran with it ON (TV_SHELL_GS_VRR defaults to 1 in the kit). The unit
# used to hard-code --adaptive-sync with no knob and no comment, silently
# answering an open design question in the direction the ops record warns
# against.
#
# The default matches the measured configuration, because that is the one we have
# evidence about. Turning it off is this one line and a session restart.
#vrr = true

# NOT YET READ BY THE CORE — the launch path does not gate on display feedback
# yet; the gate lands with the HDR-aware launch path (§6).
#
# How long GAMESCOPE_HDR_OUTPUT_FEEDBACK must read the configured value before
# an HDR-capable launch is allowed through.
#
# §6: an HDMI re-negotiation (~1 s) zeroes the HDR and VRR feedback atoms and
# then restores them — and a Vulkan surface created INSIDE that window stays SDR
# for the life of the client. So a launch is gated on the feedback atom reading
# steady for this long. The one observed instance coincided with a Moonlight
# launch to within a second and its cause is still open (§13 Q9), which is why
# this is a settle period rather than a relaunch policy.
#hotplug_settle_ms = 2000

# --- The shell's identity and the Xwayland topology ---------------------------
[session]
# The shell's own gamescope app id.
#
# PRIVATE, AND NEVER 769. Under gamescope's --steam focus policy 769 is the
# Steam client's own id (`window_is_steam`: forced fullscreen sizing,
# `focus=steam` in the stats pipe) and is reserved for the Steam client when it
# runs as an app (§5). The core REFUSES TO START on shell_app_id = 769 rather
# than let the shell inherit that path and collide with the real client the
# moment Steam launches. 9001 is the id the measurement kit used.
#shell_app_id = 9001

# How many Xwayland servers gamescope starts (--xwayland-count): one for the
# shell plus one per concurrently launched app (§4). Must be at least 1 — the
# shell needs a server.
#
# LIVE, by the same env-file link as [display] above (${TV_SHELL_GS_XWAYLAND_COUNT}).
#
# NOT `-e`. This key used to be documented as "read by the unit's ExecStart
# (-e)". `-e` is the SHORT FORM OF `--steam` — the unit was passing it twice —
# and `--xwayland-count` is long-only and takes an argument. A reader reconciling
# the doc with the unit would write `-e 2`; `-e` consumes no argument, so `2`
# becomes gamescope's child command, gamescope execs it, it fails instantly,
# BindsTo= stops the session target and the television relogins in a loop.
#
# Per-app server creation (GAMESCOPE_CREATE_XWAYLAND_SERVER) is still a later
# change; when it lands the core reads this to size the pool as well.
#xwayland_count = 2

# Bound on the base-layer read-back after a switch, FOR A WINDOW THAT IS ALREADY
# MAPPED.
#
# THIS IS THE FAILURE BOUND, NOT THE EXPECTED TIME. A switch is one
# GAMESCOPECTRL_BASELAYER_APPID write followed by a read of
# GAMESCOPE_FOCUSED_WINDOW's app id; the switch itself measured 14–19 ms over
# 20 of 20 switches (§6). 250 ms is an order of magnitude of headroom — enough
# that a slow frame, a hotplug settle or a loaded box cannot produce a false
# failure, and short enough that a real failure surfaces inside one user
# interaction instead of looking like a hang. A mismatch inside the bound is an
# IPC error, a metric and a log line — never `ok` (§5).
#
# RANGE: 1..=5000. Zero would report every switch as failed before the
# compositor could publish it. The upper bound matters just as much: an intent
# holds the core's intent lock for its whole write-and-verify, so a large value
# queues every later show/home behind it — including §9's "stuck in an app →
# intent home" recovery. A knob that can disable the escape hatch is not a knob.
#switch_timeout_ms = 250

# Bound on waiting for an app that has NOT MAPPED A WINDOW YET.
#
# A SEPARATE, MUCH LARGER BOUND, and the reason is worth reading. §6's 14–19 ms
# is the time to switch between windows that already exist. A freshly launched
# app takes SECONDS to map its first window, and for all of that time the base
# layer is correct and the compositor is behaving perfectly — there is simply
# nothing yet to show. Verified against the switch bound, `show <id>` right after
# `launch <id>` failed on every working launch, which trains a caller to ignore
# `base layer did not take` — the one error that must never be ignored.
#
# So the two waits are distinguishable: a target with a mapped window that is
# still not on screen fails as "base layer did not take" inside switch_timeout_ms
# of the window appearing; a target that never maps fails as "app <id> never
# mapped a window" after this. Both are errors; neither is ever `ok`.
#
# RANGE: 1..=300000, and never less than switch_timeout_ms.
#map_timeout_ms = 30000

# Bound on confirming that a launched process really reached its cgroup scope.
#
# A SPAWN IS NOT A LAUNCH. `systemd-run` being forked successfully says nothing
# about whether the binary exists, whether the session bus was still there, or
# whether the --unit= name collided. So after spawning, the core polls until the
# launcher is confirmed alive AND /proc/<pid>/cgroup names the scope it asked for
# — the exact string gamescope parses to resolve an app id. A launch that cannot
# be confirmed in this window is an `error:`, never a success payload naming a
# pid nothing verified.
#
# RANGE: 1..=60000. `systemd-run --scope` creates the unit before it execs, so
# the scope is normally readable within a few milliseconds; the default is
# headroom for a loaded box, not an expected time.
#launch_confirm_ms = 2000

# Bound on how long `screenshot <path>` waits for the compositor to finish a
# capture.
#
# THIS IS THE ONLY WAY TO SEE THE SCREEN UNDER V2. gamescope implements no
# Wayland screen-capture protocol, so `grim` — and every v1 surface built on it
# — cannot work here. The core instead sets gamescope's
# GAMESCOPECTRL_REQUEST_SCREENSHOT root property and waits for the FILE
# gamescope writes, then moves it to the path you named.
#
# It waits for the file and NOT for the request property, which clears long
# before the frame exists. Measured on a live 4K HDR session (gamescope
# 3.16.28): the property cleared at 32 ms, the complete PNG appeared at 712 ms.
# This bound therefore governs the slow half — one vblank-gated repaint plus a
# detached encode-and-write, with no repaint nudge from gamescope — and not the
# compositor's acknowledgement, which would be wrong by a factor of 22.
#
# The default of 5000 is ~7x the measured 712 ms.
#
# A capture that does not complete in this window is an `error:`, never a
# screenshot — and in particular never the PREVIOUS capture, which gamescope
# leaves lying at its one hardcoded output path. The core clears that path
# before every request precisely so a stale frame cannot be mistaken for a
# fresh one, and only accepts a file that is a complete PNG, since gamescope
# writes it in place and a poll can otherwise land mid-write.
#
# RANGE: 1..=30000. Raise it if captures on your hardware come back as
# timeouts — 712 ms is one machine's figure, not a universal one. Bounded above
# because a capture holds the capture lock for its whole wait, so a larger value
# queues every later screenshot behind one wedged compositor.
#screenshot_timeout_ms = 5000

# The app class the core launches and shows once, on a FRESH session. 0 = none,
# which is the default. Must name an [[app]] entry below, or the core refuses to
# start — a boot_app pointing at nothing is a black television at the end of a
# boot with an otherwise healthy core.
#
# THIS IS NOT "LAUNCH ON START", AND THE DIFFERENCE IS THE WHOLE POINT.
#
# §5/§9 forbid the core writing the base layer at startup: the core unit is
# Restart=always, so a restart under a live game is the DESIGNED recovery path,
# and relaunching there would yank the screen from something the user is playing.
# So the boot client fires on an OBSERVATION, never on "the core started": the
# reconcile must show an EMPTY base layer AND nothing on screen. A populated
# list, an app already on screen, or a read that failed are all "session in use"
# and the core keeps its hands off. That also means a core restart never
# resurrects an app you quit — after a return to the shell the base layer holds
# the shell id, so the session reads as in use.
#
# The launch happens after the IPC socket is listening, so a slow app start never
# delays the control surface an operator would use to find out why.
#boot_app = 9003

# What the boot supervisor does when the app EXITS. Durability is not just
# "reboot -> Moonlight": on this hardware a streaming client has wedged and died
# repeatedly, and a boot client that launches once leaves a black television
# until a human reboots it.
#
#   "on-failure"  relaunch only when the app exited non-zero or on a signal.
#                 DEFAULT.
#   "always"      relaunch on ANY exit — dev/gamescope/client.sh's behaviour.
#   "never"       launch once.
#
# Why the default is not "always", even though the prototype relaunches
# unconditionally: the prototype WAS the session, so a quit left a black screen
# and relaunching was the only sane answer. The default here assumes a SHELL
# behind the app, so a clean exit has somewhere to land — and relaunching over a
# deliberate quit fights the user, who pressed Quit and expects the shell. A
# crash is what durability is about, and a crash is not a clean exit.
#
# ⚠ IF YOU HAVE NO SHELL YET, SET "always". THAT IS EVERY v2 DEPLOYMENT TODAY.
#
# v2 has no shell: V2_DESIGN §13 Q1 (the shell's runtime) is still open,
# core/units/tv-shell-gamescope-child.sh is still `exec sleep infinity`, and the
# boot client below is the ONLY thing that ever puts a window on this
# compositor. So with "on-failure" and no shell, a clean quit ends supervision
# and lands on an EMPTY compositor — a black television, recoverable only from
# another machine.
#
# The default stays "on-failure" because it is right for the design and because
# a default that changed meaning between releases would silently change
# behaviour for anyone upgrading across the boundary. The deployment that is
# early carries the override:
#boot_relaunch = "always"
#
# ⚠ A SECOND REASON TO SET "always": ADOPTED APPS.
#
# When the core restarts while the boot app is running, it ADOPTS the app rather
# than relaunching it (that is what stops a restart stealing the screen from a
# live game). But an adopted app's exit status is unknowable — a status can only
# be read for a process this core forked — so under "on-failure" the core cannot
# tell an adopted crash from an adopted quit, refuses to guess, and stops
# supervising. Under "always" it does not need to know, and an adopted app keeps
# being kept alive. Since the core unit is Restart=always, a core restart is
# routine, so this is not a corner case.

# Fast-exit backoff, from dev/gamescope/client.sh:211-213 — constants chosen
# against this hardware. An app that exits within boot_fast_exit_secs of launch,
# boot_fast_exit_limit times in a row, is not going to come up (a broken runtime,
# a QML error, a client that cannot open the display), so the retry interval
# stretches from boot_relaunch_delay_secs to boot_backoff_secs instead of
# hot-spinning. Every backoff is logged at WARN with the running count: a silent
# backoff is a session that looks idle while it is actually failing.
#boot_fast_exit_secs = 10
#boot_fast_exit_limit = 3
#boot_backoff_secs = 60
#boot_relaunch_delay_secs = 2

# Give up after this many consecutive fast exits. 0 = never give up (DEFAULT).
#
# Never is the right default for an appliance: a permanent give-up guarantees a
# black television until a human intervenes, while a 60 s backoff costs nothing
# and recovers by itself the moment someone fixes the runtime — the prototype's
# own reasoning. Set a bound if you would rather fail loudly and stop; the
# give-up is logged at ERROR and says the television will show the shell until
# someone intervenes.
#boot_give_up_after = 0

# --- Supervision (§9) ---------------------------------------------------------
# NOT YET READ BY THE CORE — every key in this table. The forced-paint heartbeat
# is a later change; nothing reads these today. They are defined now so the file
# the v2 units ship against is complete rather than growing a new required key at
# deploy time.
[supervisor]
# Frames must advance at least this often or the shell counts as stalled.
#
# Measured by a FORCED-PAINT PROBE, not by tailing the stats FIFO alone:
# gamescope emits one `fps=` line per 300 paints, so on a static base layer
# under VRR the FIFO is legitimately silent for as long as nothing draws, and
# silence there is not evidence of a stall.
#stall_secs = 10

# How many restarts inside restart_window_secs count as a crash-loop rather than
# a bad day. Matches the short-session tracker shape SteamOS/ChimeraOS ship and
# the v1 daemon unit's StartLimitBurst=3 / IntervalSec=60. Must be at least 1 —
# 0 would treat the first start as a crash-loop.
#
# TO BE CLEAR ABOUT WHAT DOES NOT EXIST: §9's crash-loop response — a
# short-session tracker that backs off and then selects the v1 session — is NOT
# IMPLEMENTED. There is no ExecStartPre/ExecStopPost anywhere in core/units/ and
# no counter. The gamescope unit briefly carried StartLimitIntervalSec/Burst with
# a comment claiming it; that limiter was inert (Restart=no means there is never
# a second attempt to count, and the session script's `reset-failed` clears the
# counter on every relogin) and has been removed rather than left standing as a
# protection that is not there. Rollback today is manual: select the v1 session
# at the display manager.
#restart_threshold = 3

# The window the restart threshold is counted over, in seconds.
#restart_window_secs = 60

# ---------------------------------------------------------------------------
# [[app]] — the app-class table (V2_DESIGN §12)
# ---------------------------------------------------------------------------
#
# One entry per app the core knows how to start. `launch <appid>` over the IPC
# socket then takes BOTH the command and the environment from here, so no caller
# has to remember either — including the boot client below.
#
# WHY THE ENVIRONMENT IS PART OF THE CLASS, AND WHY `env_unset` IS NOT OPTIONAL.
#
# Measured on hardware 2026-09-06: a bare `/usr/bin/moonlight` launched inside
# the v2 session inherits, from the session the core runs in, a WAYLAND_DISPLAY
# naming gamescope's own Wayland socket plus XDG_SESSION_TYPE=wayland.
# Moonlight then selects native Wayland — which
# §6 records Moonlight 6.1.0 segfaulting on under gamescope — and never maps a
# window. The base layer is set correctly and the television stays black; the
# core's own error says so exactly ("app 9003 never mapped a window ... the base
# layer was set, so this is the app failing to start").
#
# The invocation that works removes one variable and sets three:
#
#   env -u WAYLAND_DISPLAY QT_QPA_PLATFORM=xcb SDL_VIDEODRIVER=x11 \
#       ENABLE_GAMESCOPE_WSI=1 /usr/bin/moonlight
#
# Note that one of the four operations is a REMOVAL. No value substitutes for
# absence: WAYLAND_DISPLAY="" is not the same as unset, and pressure-vessel
# rewrites an empty one back to `wayland-0` (§11). Hence `env_unset`.
#
# `command` is argv, already split — the core never invokes a shell, so there are
# no quoting rules to reason about.
#
# NOT MODELLED YET, though §12 lists them: an id strategy (scope / pid / class),
# an input contract (gamepad / keyboard), and an HDR expectation. Nothing reads
# them — the id strategy is fixed at "scope, tag as repair", §7's input layer
# does not exist, and even display.hotplug_settle_ms above is still unconsumed.
# A key whose stated consumer does not exist is the bug class this file's tests
# exist to catch, so they land with their readers.

#[[app]]
#id = 9003
#command = ["/usr/bin/moonlight"]
#env_unset = ["WAYLAND_DISPLAY"]
#
#[app.env]
#QT_QPA_PLATFORM = "xcb"
#SDL_VIDEODRIVER = "x11"
#ENABLE_GAMESCOPE_WSI = "1"

# ---------------------------------------------------------------------------
# [input] — the gamepad fleet (V2_DESIGN §7)
# ---------------------------------------------------------------------------
#
# DEFAULT OFF, DELIBERATELY. With `enabled = false` — which is what you get if
# you never write this table — the core enumerates nothing, opens nothing and
# grabs nothing. There is no code path from a disabled core to /dev/input or
# /dev/uinput at all. Deploying a core that contains this layer therefore changes
# nothing on a box until someone edits this file, which is the point: on this
# hardware the pad reaching Moonlight is the thing that must not break.
#
# TO TURN IT OFF AGAIN: set `enabled = false` and restart the core. That is
# sufficient and total. An EVIOCGRAB lives on an open file descriptor, so every
# grab is released when the process exits — by SIGTERM, by SIGKILL, by a panic —
# and the uinput presenters disappear with it. `systemctl --user kill
# tv-shell-core` releases your controllers just as completely as a clean stop;
# there is no state left behind to clean up.
#
# WHAT THIS LAYER DOES TODAY: claims every controller the database recognises,
# assigns each a stable player slot, and re-presents it on a permanent per-player
# virtual pad. On its own that is BEHAVIOURALLY INVISIBLE — what reads the
# presenter sees the same input the physical pad was sending.
#
# IT ALSO OWNS THE GUIDE BUTTON, on every route. A Guide TAP goes to whatever is
# on screen; a Guide HOLD past `guide_hold_ms` makes the CORE write the base
# layer back to the shell itself — no message to the shell, so it works with the
# shell dead, hung or never started. That is the one thing v1 got wrong here: it
# delivered this escape as an intent the shell acted on, and it failed exactly
# when the shell was wedged. See jedwards1230/tv-shell#496.
#
# IT ALSO DECIDES WHO OWNS THE PAD. A watcher reads what is on screen roughly
# four times a second (and immediately after the core's own `show`/`launch`/
# `home`, including the Guide escape's) and routes accordingly: the shell's app
# id means the pad drives the shell as keys, any other app means the pad reaches
# that app through its presenter. A change is applied only once it has held still
# for 300 ms, which collapses the focus flap every launch produces into one
# transition. If it cannot tell — nothing is mapped, or the screen could not be
# read — THE PAD GOES TO THE APP, never to the shell: a game on screen and a dead
# controller is the worse failure.
#
# The shell can also declare an input-taking overlay (a drawer, the QAM) with the
# `input-focus take` / `input-focus release` IPC verb. It is a declaration of the
# shell's own state, not a command about routing — the core still decides — so a
# shell that dies without releasing self-heals as soon as something else is on
# screen.
#
# WHAT IT DOES NOT DO YET: MASK a button held across a route change. Each
# transition *quiesces* the target it leaves, so nothing is left holding a
# button — but the physical release that arrives afterwards still crosses to the
# new target, which is jedwards1230/tv-shell#295's shape. Rumble, battery, LEDs
# and companion touchpad/motion-node inhibition are also still to come. See
# docs/V2_GAMEPAD_HANDOFF.md.

#[input]
# The safety flag.
#enabled = false
#
# PIN the pad to the SHELL, disabling owner arbitration. Off by default, and
# inert unless `enabled` is on too.
#
# READ THIS BEFORE SETTING IT. With this on the route never changes, whatever is
# on screen: EVERY pad is grabbed and NO app or game receives pad input, so
# Moonlight loses the controller for as long as the core runs with this set. It
# is the operator override for taking a measurement, not the way to use the
# shell — leaving it OFF is what gives you a pad that drives the shell at the
# home screen and the app once one is running.
#
# What the shell route does: A activates (KEY_ENTER), B goes back (KEY_ESC),
# Start toggles the drawer (KEY_TAB), and the d-pad and left stick drive the
# arrows — the stick with v1's calibrated auto-repeat (300 ms, then every
# 150 ms). The Guide button is deliberately NOT a key — see `guide_hold_ms`. Only
# key codes measured to actually reach the shell through gamescope are emitted:
# KEY_MENU is dropped by gamescope silently, which is why the drawer is Tab.
#
# The keyboard device `tv-shell-keys` is created alongside the presenters
# whenever `enabled` is on, whether or not this flag is set — once at startup and
# never again, the same permanence rule the presenters follow, and gone with the
# process. It has to exist up front precisely BECAUSE the route can change: a
# device that appeared at the moment of a transition would be a hotplug event
# every game and Moonlight forward to the streaming host
# (jedwards1230/tv-shell#402).
#shell_keys = false
#
# Player slots, and so how many permanent uinput presenters exist. A CAPACITY,
# not a count of connected pads: the presenters are created at startup whether or
# not anyone is holding a controller, because a presenter that appeared when a
# pad did would be a hotplug event to every game and to Moonlight — which is
# exactly the defect jedwards1230/tv-shell#402 records. 1-8.
#players = 4
#
# The Guide tap-vs-hold threshold, in milliseconds. v1's `meta_hold_ms`, ported
# with its value: 500 ms is a press-and-hold a deliberate long press clears and a
# quick tap does not. Below the threshold the button goes to whatever is on
# screen (a real Guide press+release to an app, the drawer key to the shell);
# past it the core writes the base layer back to the shell itself.
#
# This is ACTIVE WHENEVER `enabled` IS ON — it is not gated behind `shell_keys`,
# because it is the way back from a running app and that is precisely the case
# where the shell route is off. `input-state` reports it under `escape`: whether
# a hold is armed, how many fired, and how many fired and could not be
# delivered. 100-5000 ms.
#guide_hold_ms = 500
#
# How often the fleet is re-enumerated. Polled rather than driven by a udev
# listener, per V2_DESIGN §10: v1's residual defect was an attached listener that
# processed nothing, and a poll that stops has no equivalent quiet failure mode.
# 100-60000 ms.
#poll_interval_ms = 2000
#
# An SDL_GameControllerDB file layered OVER the bundled baseline, for a
# controller the baseline does not know. A path that cannot be read, or a file
# that parses to zero entries, is an ERROR and not a silent fallback — otherwise
# a wrong path reads as "my controller is unsupported".
#controller_db = "/etc/tv-shell/gamecontrollerdb.txt"
#
# Operator pin: claim ONLY this vendor/product, bypassing the database entirely.
# The escape hatch for a pad no database knows. It EXCLUDES as well as includes —
# with a pin set, database-known pads that are not the pinned model are refused —
# so it is a whitelist of one, not a hint. Both keys or neither; a half-pin is
# rejected at startup.
#pin_vendor = 0x045e
#pin_product = 0x028e
